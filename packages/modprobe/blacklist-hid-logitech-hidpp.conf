# Blacklist the hid_logitech_hidpp driver.
# On some Logitech mice this module breaks the scroll wheel; blacklisting it
# makes the kernel fall back to the generic HID driver and restores scrolling.
# Installed to /etc/modprobe.d/ by 'setup post'. Requires an initramfs rebuild.
blacklist hid_logitech_hidpp
