# AMD GPU drivers (open source, included in Mesa).

mesa                # OpenGL and Vulkan implementation for AMD (radv driver)
vulkan-radeon       # AMD Vulkan driver (radv)
libva-mesa-driver   # AMD VA-API hardware video decode/encode
