# Intel GPU drivers (open source, included in Mesa).

mesa                # OpenGL and Vulkan implementation
vulkan-intel        # Intel Vulkan driver (ANV)
intel-media-driver  # Intel VA-API hardware video decode/encode (iHD driver, Gen8+)
