# Personal applications — all packages in one place.
#
# Official-repo packages come first; AUR-only packages follow after the marker.
# Both sections are installed by post-install.sh using paru/yay (handles both
# transparently). The archinstall step (install.sh) only injects packages
# above the "# AUR only" line into its JSON config.

# ══════════════════════════════════════════════════════════════════════════════
# Official repos
# ══════════════════════════════════════════════════════════════════════════════

## File management
less                # Terminal pager (man pages, pipe output)
ffmpeg              # Video thumbnail generation for file managers
poppler             # PDF rendering library (yazi PDF preview)
unzip               # ZIP archive extraction

## Image and media
viewnior            # Simple, fast image viewer
mpv                 # Video player supporting most formats

## System monitoring
btop                # Resource monitor with rich TUI

## CLI utilities
tree                # Directory tree viewer
wget                # File downloader
jq                  # JSON processor and query tool
ripgrep             # Fast grep replacement (rg)
fzf                 # General-purpose fuzzy finder
zoxide              # Smarter cd with frecency ranking
resvg               # SVG renderer (used by yazi for SVG preview)
imagemagick         # Image conversion and manipulation
rsync               # Fast incremental file sync
bc                  # Arbitrary precision calculator
iwd

## Build tools (required to build AUR packages with paru/yay)
git                 # Version control system
base-devel          # GCC, make, and other tools needed by makepkg

## Communication
discord             # Discord messaging and voice client (official repo)

## Audio
cava                # Console-based audio visualizer (bar spectrum)

## Shell utilities
eza                 # Modern ls replacement (used by the fish config)

## Notes
obsidian            # Markdown knowledge base and note-taking app

## Terminal file manager
yazi                # Blazing-fast terminal file manager with preview support

## Shell history and system info
atuin               # Shell history with sync, search, and statistics
fastfetch           # Fast system information display (neofetch alternative)
fish                # User-friendly interactive shell
starship            # Minimal, fast cross-shell prompt

# ══════════════════════════════════════════════════════════════════════════════
# AUR only — everything below this line is NOT injected into archinstall
# ══════════════════════════════════════════════════════════════════════════════

## File manager extras
unarchiver                 # Archive extraction CLI (unar) — supports many formats

## Version manager
asdf-vm                    # Multi-language runtime version manager

## Browsers
brave-bin                  # Brave browser (privacy-focused, Chromium-based)
zen-browser-bin            # Zen Browser (Firefox-based, privacy-focused)
google-chrome              # Google Chrome browser

## Code editor
visual-studio-code-bin     # VS Code (official Microsoft binary release)

## Gaming
steam                      # Steam gaming platform (requires multilib)
protonup-qt                # GUI tool for managing Proton-GE and Wine-GE

## Communication
# equicord-installer-bin     # Equicord installer CLI (Equilotl) — Discord mod + OpenAsar

## Image upscaling
upscayl-bin                # GPU AI image upscaler (Real-ESRGAN based)

## File sharing
localsend-bin              # Cross-platform local file sharing (AirDrop-like, no internet)

## Music
# YouTube Music as a desktop app.
#
# Use the AUR package, NOT the release tarball: the tarball has to be unpacked
# into /opt by hand along with a hand-written .desktop, and that .desktop ends up
# named after the app rather than its WM class. The bar looks icons up by class,
# so the mismatch sends every lookup through AppSearch's fuzzy fallback — which
# is what made the workspace animation stutter while the app was open.
#
# The AUR package installs glassy-music-nankill-mod.desktop, matching the class
# exactly, and links against the system electron instead of bundling its own.
glassy-music-nankill-git   # Glassy Music — YouTube Music desktop client
